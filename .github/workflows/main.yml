name: Build
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install
        run: pip install flet
      - name: Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
      - name: Build
        run: flet build apk --project "MyAccount" --org "com.sun"
      - name: Export
        uses: actions/upload-artifact@v4
        with:
          name: my-apk
          path: build/apk/*.apk
